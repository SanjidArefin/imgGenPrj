# imgGenPrj
## University Final year project.

Developed an image-to-video generator, which uses a pre-trained model with SSIM-guided video-specificf refinement to produce an “action frame” from which it  extracts the algorithm to populate the remaining frame.
